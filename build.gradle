buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:0.13.2'
    }
}

ext {
    compileSdkVersion = 17
    buildToolsVersion = "19.1.0"
}

allprojects {
    repositories {
        mavenCentral()
    }
}
