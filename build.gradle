buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:1.1.0'
    }
}

ext {
    compileSdkVersion = 21
    buildToolsVersion = "19.1.0"
}

allprojects {
    repositories {
        mavenCentral()
    }
}
