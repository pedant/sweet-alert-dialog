buildscript {
    repositories {
        mavenCentral()
        google()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.2.1'
    }
}

ext {
    compileSdkVersion = 30
    buildToolsVersion = "30.0.2"
}

allprojects {
    repositories {
        mavenCentral()
        google()
    }
}
